
Congratulations <%= @user.name %> Your order is on its way!
===========================================================

<% @order.line_items.each do |item| %>
          <%= render 'item_text', product: Product.find(item[:product_id]), quantity: item[:quantity].to_i %>
        <% end %>
        
 Total price: <%= @order.total_cents / 100.0 %>
 
Thanks for ordering from Jungle, have a great day!
